{
  "name": "onepieceql",
  "version": "1.0.0",
  "description": "One Piece GraphQL",
  "scripts": {
    "docker:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
    "docker:build": "docker compose up -d --build --no-deps"
  }
}
